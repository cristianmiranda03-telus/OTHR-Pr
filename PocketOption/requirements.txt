# Local install:
#   pip install .
#   pip install -e .
#   pip install ".[notebook]"
# Email/password auto-login (Chromium):
#   pip install ".[credentials]"
#   playwright install chromium
#
# Cliente comunitario (no oficial Pocket Option):
# https://chipadevteam.github.io/PocketOptionAPI/
pocketoptionapi-async>=2.0.1

# Notebook / analysis
jupyter>=1.0.0
ipykernel>=6.0.0
nest-asyncio>=1.5.0
python-dotenv>=1.0.0
pandas>=2.0.0
